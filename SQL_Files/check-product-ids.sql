-- First, let's see what the actual product IDs are in your database
-- Run this query first to get the correct UUIDs:

SELECT id, name FROM products ORDER BY name;

-- Then use those UUIDs in the INSERT statements below
-- Replace the UUIDs in the INSERT statements with the actual UUIDs from the query above
